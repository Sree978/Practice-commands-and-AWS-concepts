Jenkins > Manage jenkins > System > Email notification SMTP server add  smtp.gmail.com
advanced > SMTP authentication > user name : our mail id (which will prefer to get notification) & enable use SSL 
& pass word Google account password  and enabl etwo step verification 
SMTP port number : 465
add mail and test mail verification 
Jenkins > pipeline > add script ntg but stages 

Pipeline - Post Build Actions 
pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Hello build World'
            }
        }

        stage('code') {
            steps {
                echo 'Hello code World'
            }
        }

        stage('test') {
            steps {
                echo 'Hello test World'
            }
        }
    }
post {
        success {
            mail to: 'sreesanav123@gmail.com',
            subject: "Build Status: ${currentBuild.fullDisplayName}",
            body: "The build ${env.BUILD_NUMBER} finished with status: ${currentBuild.currentResult}. View logs at: ${env.BUILD_URL}"
        }
    }
}

check mail to get update 
