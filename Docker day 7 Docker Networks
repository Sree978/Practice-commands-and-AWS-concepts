Docker networks 
to communcate docker to docker or stop communication in between docker will use docker networks 
4 types 
1)bridge   - its default network while create containers
2)host
3)none
4)overlay 
docker network ls --- to check list of networks

create 2 to 3 containers
cmd : docker inspect cont1   --- to know about particular object (ex : container)
docker inspect cont1 | -i "network" / docker inspect cont1 | grep -i "ip"  -- to get particular details
now we have to check either ex 2nd container ip pinging in 1st container, 1st conatiner ip pinging in 2nd container
will do in two ways
inside conatiner & outside container also
we have to install yum install iputils -y for every container ping 
docker exec -it cont1 apt update
docker exec -it cont1 apt install iputils-ping -y 
docker exec -it cont1 ping 172.17.0.3
docker exec -it cont1 touch filename ---- to create a file
docker exec -it cont1 mkdir sree  -- to creat folder 
docker run -itd --name cont4 -p 4444:80 --network=host sree978/dm   -- to set host network 


None network : to keep conatiner private dont want to expose 
docker run -itd --name cont6 -p 8888:80 --network=none nginx
we never access this    will use mostly for databases or sensitive containers 

 docker network create frontend (networkname)    ----- to customize our own network 
 docker network create backendend (networkname) 
 front end containers can't communicate with backend caonatiner 

docker run -itd --name frontend-cont -p 1112:80 --network frontend nginx
docker run -itd --name backend-cont -p 1122:80 --network backend nginx

we have make to cummunicate each other 
 goto frontend container : 
docker exec -it frontend-cont bash
install ping : apt update
 apt install iputils-ping -y 
and chech ping of backend conatiner it won't work 

same for backend 
docker exec -it backend-cont bash
install ping : apt update
 apt install iputils-ping -y
and chech ping of frontend conatiner it won't work 

all connatiners are communicate its they are in same network 
wont communicate diffirent containers in diffierent network

so for connect it

 docker network connect frontend backend-cont
 docker network connect backend frontend-cont

now check either communicating or not 
go to frontend network and ping backend ip 


to disconnect 

docker network disconnect backend frontend-cont














