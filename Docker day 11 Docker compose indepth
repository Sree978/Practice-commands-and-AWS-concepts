create ec2 server and isntall docker & compose 
cmd: vi compose.yml   ---create compose file

mkdir myapp
    3  cd myapp/
    4  vi compose.yml
    5  mkdir flight bus movie
    6  cd bus/
    8  vim index.html
   10  vim Dockerfile
   11  ll
   14  cp bus/* flight
   15  ll
   17  cp * ../movie
   18  cp * ../flight
   19  cd ..
   20  cd flight/
   21  vim index.html
   22  cd ..
   23  cd movie/
   24  vim index.html
   25  cd  ..
   26  yum install tree -y
   27  tree
   28  vim bus/Dockerfile
   29  vim flight/Dockerfile
   30  vim movie/Dockerfile
   32  clear
   33  docker build -t busimage bus/
   34  clear
   35  docker build -t fightimage flight/
   36  docker build -t movieimage movie/
   37  clear
   38  docker images

vim compose.yml

services:
  bus_ticket:
    container_name: bus-container
    image: busimage
    ports:
      - "1111:80"

  flight__ticket:
    container_name: fight-container
    image: flightimage
    ports:
      - "2222:80"

  movie_ticket:
    container_name: movie-container
    image: movieimage
    ports:
      - "3333:80"
 docker-compose up -d

will notice 3 containers created 

and access all three ports with same ip 
if we update anything in index.html it wont reflect automatically again we have to build image and re create container then its reflecting
to over this we have make changes in compose file taht we wont give image

[root@ip-172-31-92-65 myapp]# vim compose.yml
services:
  bus_ticket:
    container_name: bus-container
    build: ./bus/
    ports:
      - "1111:80"

  flight__ticket:
    container_name: fight-container
    build: ./flight
    ports:
      - "2222:80"

  movie_ticket:
    container_name: movie-container
    build: ./movie
    ports:
      - "3333:80"

docker-compose build    ---- will create 3 images at a time
docker-compose up -d   ----- will create 3 conatiners at at ime

if will make change in changes in particular folder and run above commands its reflect automatically

for create volumes and networs

services:
  bus_tickets:
    container_name: bus_container
    build: ./bus/
    ports:
      - "1111:80"
    networks:
      - bus_network
      - flight_network
    volumes:
      - bus:/opt/mustafa

  flight_tickets:
    container_name: flight_container
    build: ./flight
    ports:
      - "2222:80"
    networks:
      - flight_network
      - bus_network
    volumes:
      - flight:/etc/flm

  movie_tickets:
    container_name: movie_container
    build: ./movie
    ports:
      - "3333:80"
    networks:
      - movie_network

networks:
  bus_network:
    driver: bridge

  flight_network:
    driver: bridge

  movie_network:
    driver: bridge

volumes:
  bus:
  flight:

again we have to run docker-compose up-d





