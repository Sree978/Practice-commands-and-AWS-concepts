Get code from deep seek    (example : generate code for bus ticket booking platform using html css and jscript make sure UI should clear and more attcractive by customers and user friendly)
create a folder (mkdir bustickets)
vim index.html    -- to write a code
copy paste code whic we get from deepseek 
get webservers nginx or httpd
---> install we servers and copy sourse code to default path of webserver ( /var/share/nginx/html)
we have to write docker file (FROM nginx
COPY index.html /usr/share/nginx/html
)

create image & container 
check conatiner started or not 
check over browswer with ip address

------> Python application <---------
Create a folder and one touch app.py --> to store code
    Touch requiremnts.txt
we have to write docker file for both .txt and.py
dockerfile 
FROM python
WORKDIR /myapp
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
next create image and container 
finaly try over browser


Node js

FROM node
WORKDIR /myapp
COPY package*.json .
RUN npm install
COPY . .
CMD ["node", "index.js"]





