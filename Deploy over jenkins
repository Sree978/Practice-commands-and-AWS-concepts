Step 1
create ec2 & connect over terminal 
install jenkins and connect over browser 
clone repo which it in git 
check over workspce either its reflecting or not 
build > we have to install maven 
will integrate maven over Jenkins 
Manage jenkins > tools > maven installation > name is our wish   
go to jenkins > config > build steps >invoke top- level maven targets 
slect name which we created & Goals mention clean package / clean install & click on build now once it success 

open work spce will notice target folder in side target folder will notice war/jar file 

we have to integrat war/jar file over application server 

create ec2 and install tomact  (binary way installl)  path : https://dlcdn.apache.org/tomcat/

have to copy link
example : wget https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.117/bin/apache-tomcat-9.0.117.tar.gz (tomact)
unzip :  tar -zxvf apache-tomcat-9.0.117.tar.gz

for start service we have to go bin folder
./startup.sh   - for excute file 
connect over browser with ipa address of ec2 will get tomcat server

403 Access Denied error  
cd .. > [root@app-serve apache-tomcat-9.0.117]#
to find context.xml file    (cmd : find . -name context.xml)
cd ./webapps/manager/META-INF/context.xml
vim context.xml  delete 21 and 22 line 

valve classname .........................
allow ......................

401 Unauthorized

 conf/tomcat-users.xml   login should in in this path
vim tomcat-users.xml 
<role rolename="tomcat"/>     replace tomcat <> manager-gui
  <role rolename="role1"/>    replace role1 <>manager-script
  <user username="tomcat" password="<must-be-changed>" roles="tomcat"/>   username : tomat password : admin@123
        roles : manger-gui, manager-script
these things we have to over error rules 

start /stop tomcat we have to to go bin folder
once chnaged config we have to restart  tomcat
go to bin folder and restart 
cmd :   ./shutdown.sh
start again anad check UI 





