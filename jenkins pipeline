Go to jenkins dashboard 

Pipline two types 
1) scripted
2)declarative  

declarative
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'   - we have to mention what action we have to do here
            }
        }
    }
}
P --> pipeline  
A --> agent  --  which slave server its runs 
S--> stages  
S --> stage  
S --> steps  

pipeline

code    build    test    deploy

stage  (pointing to each box)

stages (pointing to all boxes)
stage : code & build 
stages : code ,build,test & deploy 
pipeine : agent code build test deploy 
step : only one step for pipeline 

pipeline {
    agent any

    stages {
        stage('code') {
            steps {
                echo 'pipeline delcarative type'
            }
        }
        stage('test ') {
            steps {
                echo 'pipeline  type'
            }
        }
    }
}


Manage jenkins > plugin>available plugin ? pipeline stage view

for workspace we have to go  default path    cd /var/lib/jenkins/

cd workspace 

to get code use pipineline syntax >Steps
Sample Step > git > add repo > generate pipeline script 
generated script we have to add in steps of pipeline 

check either got code or not 

---- build  ------

jenkins > pipeline > configure > pipeline > after agent any we have to add like this 
manage jenkins >tools> install maven > name : mymaven or any name ourwish 
 agent any
    tools {
        maven 'mymaven'
    }

&  add stage after a satges 

stage('build') {
            steps {
                sh " mvn clean package"
            }
    }
and build it's success 

will notice target folder 
inside target folder will notice war file 

------deploy -----

create a application server (tomcat) connect over browser 
and back to pipeline  for deploy step use pipeline syntax for script >sample step > deploy war/ear to a container (Restart jenkins didn;t see this optioneven after istallation of plugin)
install plugin > available plugin > deploy to container install it 
WAR/EAR files : target/*war

Context path ;myapp

and tomcat credentails 

post that build it its will success after to tomact UI and find myapp application 

add stage stage('deploy') {
            steps {
                deploy adapters: [tomcat9(
                    alternativeDeploymentContext: '',
                    credentialsId: 'tomcatadmin',
                    path: '',
                    url: 'http://54.163.18.211:8080'
                )],
                contextPath: 'myapp',
                war: 'target/*.war'
            }
        }

-------Nexus--------

create a nexus server and connect ove browser
 sonar application > settings > create a repository 

select type maven2(hosted)
name : myrepo our wish 
deployment policy :allow redoploy 

nexy=us integrate withjenkins pipeline 
we need one plugin  nexus artifact uploader 
will add in pipeline after build stage 


pipline syntax and get step 
pipine synatx sample text > nexus artifact uploader 
select version and http 
add nexus url and add credentails 

groupid :
version
repository 
get all thesee from pom.xml in git repo 
git >repo which we selected and master branch 
group id  & version will get from pom xml file 
repositoy : which we created in nexus same name















